import styled from '@emotion/styled';

export const AppWrapper = styled.div`
    width: 100vw;
    height: 100vh;

    background: linear-gradient(180deg, #77aaff, #6699ee);

    display: grid;
    place-items: center;
`;
